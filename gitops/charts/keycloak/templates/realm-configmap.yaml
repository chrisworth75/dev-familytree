apiVersion: v1
kind: ConfigMap
metadata:
  name: keycloak-realm
  labels:
    app: keycloak
data:
  # Synced from infra/keycloak/realm-export.json (single source of truth).
  realm-export.json: |-
{{ .Files.Get "files/realm-export.json" | indent 4 }}

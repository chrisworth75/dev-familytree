apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: api
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"   # after postgres + keycloak
spec:
  project: default
  source:
    repoURL: https://github.com/chrisworth75/dev-familytree.git
    targetRevision: main
    path: gitops/charts/api
  destination:
    server: https://kubernetes.default.svc
    namespace: family-tree
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

{
  "name": "family-tree-tests",
  "version": "1.0.0",
  "description": "Playwright tests for Family Tree app",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
